/**
 * Local Backend Definition
 * Empty stub for local-first mode - no remote backend operations needed
 */

import type { BackendPlatformDef } from "@auk/common/src/platform/backend"

export const def: BackendPlatformDef = {
  // No backend operations needed in local-only mode
}
