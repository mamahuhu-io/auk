// Backend platform definition - simplified for local-only mode
// All backend API calls have been removed as part of the local-first architecture

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export type BackendPlatformDef = {
  // No backend operations needed in local-only mode
}
