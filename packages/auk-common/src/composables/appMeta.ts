// Use the desktop package version so the UI reflects the installed desktop app build
import desktopPackage from "../../../../packages/auk-desktop/package.json"

export const appVersion = desktopPackage.version
