// Standard backend implementation - simplified for local-only mode
import { BackendPlatformDef } from "~/platform/backend"

export const def: BackendPlatformDef = {
  // No backend operations needed in local-only mode
}
